{
  "name": "centy-daemon-e2e",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for Centy Daemon gRPC API",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "daemon:build": "cd .. && cargo build --release",
    "daemon:start": "cd .. && cargo run --release"
  },
  "devDependencies": {
    "@grpc/grpc-js": "^1.12.2",
    "@grpc/proto-loader": "^0.7.13",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.24.0"
}
